name: feature_auth
description: 인증 feature — 로그인/로그아웃, 토큰 관리, 인증 상태. 라우터 가드는 앱이 이 패키지의 상태를 구독해 구성한다.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  app_core:
  app_design_system:
  app_l10n:
  app_mvi:
  app_network:
  app_storage:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  freezed_annotation: ^3.1.0
  go_router: ^17.5.0
  riverpod_annotation: ^4.0.0
dev_dependencies:
  build_runner: ^2.15.1
  custom_lint: ^0.8.1
  flutter_test:
    sdk: flutter
  freezed: ^3.2.3
  mocktail: ^1.0.5
  riverpod_generator: ^4.0.0+1
  riverpod_lint: ^3.1.0
