name: app_mvi
description: >-
  MVI 프레젠테이션 기반 계층 — Intent/State/Effect 계약, Effect 방출 mixin,
  Effect 리스너 위젯. 모든 feature의 presentation은 이 패키지의 규약을 따른다.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
dev_dependencies:
  custom_lint: ^0.8.1
  flutter_test:
    sdk: flutter
  riverpod_lint: ^3.1.0
