name: app_storage
description: 로컬 저장소 계층 — key-value(prefs), 보안 저장소(secure storage), 로컬 DB(drift).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  app_core:
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  flutter_secure_storage: ^11.0.0
  riverpod_annotation: ^4.0.0
  shared_preferences: ^2.5.5
dev_dependencies:
  build_runner: ^2.15.1
  custom_lint: ^0.8.1
  drift_dev: ^2.31.0
  flutter_test:
    sdk: flutter
  riverpod_generator: ^4.0.0+1
  riverpod_lint: ^3.1.0
